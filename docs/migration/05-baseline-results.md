# Línea base React

- Node 24.14.1; pnpm 11.3.0.
- Dependencias en el origen no ejecutables: `pnpm exec vite` y `pnpm exec tsc` no encontraron binarios.
- Se copió el código/configuración a `baseline-react` dentro del workspace de conversación, incluyendo `.figma` requerido por Vite. No se editó el origen.
- Instalación `pnpm install --frozen-lockfile`: correcta (51 paquetes).
- Build y comprobación TypeScript: en ejecución al crear este inventario; se actualizará con su resultado.
- No existen scripts de lint/test en el original; no se declara que hayan pasado.
- Comparación visual y consola: pendiente de ejecutar.
